# Streamlit Secrets Configuration Example
# Copy this file to secrets.toml (for local development) or use Streamlit Cloud Secrets

# DEMO MODE: Set to "true" to use mock/demo data (no API key required)
# This is perfect for showcasing the UI without needing a real API key
DEMO_MODE = "true"

# OR: Use a real API key for production
# Get your API key from: https://makersuite.google.com/app/apikey
# GEMINI_API_KEY = "your_api_key_here"

# Optional: Gemini model name (default: gemini-pro)
# GEMINI_MODEL = "gemini-pro"

# Optional: Maximum retry attempts for API calls (default: 3)
# MAX_RETRIES = "3"

# Optional: API timeout in seconds (default: 30)
# TIMEOUT = "30"

# Note: For Streamlit Cloud deployment:
# 1. Go to your app's Settings → Secrets
# 2. To use DEMO MODE: Set DEMO_MODE = "true" (no API key needed)
# 3. For production: Set GEMINI_API_KEY = "your_actual_key" and DEMO_MODE = "false"
# 4. Save and the app will redeploy automatically

