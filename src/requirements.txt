# requirements.txt
# This file lists all Python dependencies required for the AI-Powered Travel Planner Assistant project.
# Install these dependencies using pip: pip install -r requirements.txt

# --- Core Application Dependencies ---
Flask>=3.0.0             # Web framework for the Applications Layer (backend)
Flask-Cors>=4.0.0        # For handling Cross-Origin Resource Sharing in Flask
httpx>=0.27.0            # Asynchronous HTTP client for API calls (e.g., Gemini API)
python-dotenv>=1.0.0     # For loading environment variables from .env files
PyYAML>=6.0.1            # For parsing YAML configuration files (if used, e.g., for complex data configs)

# --- AI/LLM Dependencies ---
# The Gemini API is called via direct HTTP requests using httpx, so no specific client library is strictly needed here
# If using Google's official client library, uncomment:
# google-generativeai>=0.5.0

# --- Development and Testing Dependencies (install with pip install -r requirements.txt for dev) ---
pytest>=8.0.0            # Testing framework
pytest-asyncio>=0.23.0   # For testing asynchronous code with pytest
flake8>=7.0.0            # Linter for enforcing style guide (PEP8)
black>=24.0.0            # Uncompromising Python code formatter
